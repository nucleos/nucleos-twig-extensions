framework:
    secret: secret

twig:
    paths:
        - '%kernel.project_dir%/templates'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    Nucleos\Twig\Tests\Bridge\Symfony\App\Controller\TwigTestController:
      tags:
        - controller.service_arguments
